# services/notifier (NOT BUILT — A7.3)

FCM push fan-out + email. Async, fan-out shaped, third-party latency (§3.2). Built in **A7.3 (processing
notifications)**. Operating cost: one async Cloud Run service + queue + a dashboard/alert.
