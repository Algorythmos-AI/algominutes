{
  "name": "@algominutes/ai",
  "version": "0.1.0",
  "private": true,
  "description": "AlgoMinutes AI + shared runtime libs: schema-constrained Gemini calls with retry ladder, PII pre-scrub, prompt/summary templates, embeddings, cloud-tasks, structured logger, pg-query, storage-paths. Holds all of the source shared/ so services resolve one package (@algominutes/ai/<name>).",
  "license": "UNLICENSED",
  "exports": {
    "./*": "./src/*"
  },
  "dependencies": {
    "@google-cloud/aiplatform": "^3.34.0",
    "@google-cloud/tasks": "^5.5.0",
    "google-auth-library": "^9.15.1",
    "pg": "^8.23.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
