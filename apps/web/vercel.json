{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "headers": [
    {
      "//": "A10 share links: shared-note pages must never be indexed. Defence in depth alongside the in-app <meta robots> (SharedNote.tsx), robots.txt Disallow, and the API's X-Robots-Tag on the data response. If the web host is not Vercel, replicate this X-Robots-Tag on /s/** there.",
      "source": "/s/(.*)",
      "headers": [
        { "key": "X-Robots-Tag", "value": "noindex, nofollow" },
        { "key": "Cache-Control", "value": "no-store" }
      ]
    }
  ]
}
